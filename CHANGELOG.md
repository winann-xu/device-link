# Changelog

## v1.0.11 (2026-09-22)

### 新功能：历史数据自动清理（默认只保留 30 天）

- 新增 `src/storage/maintenance.py` 与后台清理线程：启动即执行一轮，之后每
  `storage.cleanup.interval_minutes`（默认 60 分钟）一轮，删除超过保留期
  （默认 30 天）的 `status_history` 记录
- **分批删除**：每批 `batch_size`（默认 5000）行、批间休眠 `batch_sleep_ms`（默认 50ms）
  并释放写锁；单轮上限 `max_rows_per_run`（默认 200 万行）。老库首次上线积压的
  过期数据分多轮清完，探测落库与界面不受影响
- 同步清理超过保留期**且已确认**的 `alert_events`（未确认事件保留，不影响告警升级），
  以及超过保留期的日汇总行 `status_daily`
- 清理后自动 WAL checkpoint(TRUNCATE) + 增量回收 + `PRAGMA optimize`；
  `journal_size_limit` 设为 64MB —— 解决长期运行 `-wal` 文件膨胀问题
  （实测生产库主库 2490.8MB + `-wal` 5711.7MB（合计 8.2GB），checkpoint 后 1.1s 回收 5.58GB）
- 新库自动启用 `auto_vacuum=INCREMENTAL`：清理掉的空闲页可增量归还文件系统
- 新增离线维护命令 `DEVICE-LINK.exe --vacuum-now`：清理全部过期数据并 VACUUM 整库
  回收磁盘（需先退出正在运行的程序；检测到数据库被占用时拒绝执行并返回码 1）
- 旧配置兼容：仍是旧版出厂默认 90 天的配置按新策略 30 天执行并写 WARNING；
  显式配置 `storage.cleanup.retention_days` 则完全尊重用户设置
- 每轮结果写入 `logs/retention_state.json`（原子替换），日志记录删除行数与库大小

### 新功能：历史统计日汇总表 status_daily（历史页数据源）

- 背景：1143 台设备 120 秒间隔约 88 万行/天，30 天窗口近 2600 万行；
  历史页每次刷新都要在这些行上做范围统计，索引优化后仍需 1~2 秒，
  设备更多/间隔更短时不可用
- 新增 `status_daily` 表（设备×日期一行：total/online/offline/数量 + 延迟合计，
  WITHOUT ROWID）：每次探测在**同一事务内**累加当日计数
- 历史页统计（今日/7天/30天在线率、离线时长排行榜）改为读这张小表：
  1143 台 × 30 天 ≈ 3.4 万行，实测历史页刷新 4.5ms（1728 万行基准库）
- 日汇总缺失时（例如直接写历史表的旧数据）自动回退扫描 `status_history`，口径不变
- 老库首次升级自动回填整个保留期窗口（一次扫描，幂等覆盖），之后每轮只重算最近
  2 天（`daily_stats_rebuild_days`），修复旧版本运行期间漏记的计数
- 写入代价：单次探测多一次小型 upsert，实测 +0.73ms/次（200 台设备 30 秒间隔约
  6.7 次/秒，可忽略）

### 优化：历史统计索引与查询

- 新增 `idx_sh_checked_status(checked_at, status)`：服务保留期分批清理
  （按 `checked_at` 范围删）与在线率回退统计——原库只有 `(device_id, checked_at)`
  索引，前导列不匹配导致"全部设备"统计只能全表扫描
- 新增 `idx_sh_offline(device_id, checked_at) WHERE status='offline'` 部分索引：
  只索引离线行（占比极小），离线时长统计从 38.7s 降到 0.02s
- `alert_events` 新增 `idx_ae_created_at` / `idx_ae_pending_escalation` / `idx_ae_device_created`
- 启动时自动补建缺失索引并记录耗时（大库一次性代价）；不额外增加宽覆盖索引，
  避免与"data 目录过大"的诉求冲突
- 离线时长排行榜查询改为"离线行聚合派生表 + 关联设备"写法：旧 LEFT JOIN 写法
  在缺宽索引时优化器会退化成按设备扫全部行（1728 万行库实测 49.8s），
  新写法稳定命中部分索引，0.015s
- 统计回退路径用两次 `COUNT(*)` 而非 `SUM(CASE)` 条件聚合：实测覆盖索引下
  COUNT 是纯索引区间计数（30 天窗口 1.17s），SUM(CASE) 要对每行求表达式（~2 倍耗时）
- `query_status_range` 增加 `id` 次序键，修复同秒记录顺序不稳定的问题

### 测试

- 新增 `tests/codex/test_retention_and_index.py`（49 项）：保留期配置解析与旧配置兼容、
  分批删除、告警事件保留口径、索引存在性/幂等/`EXPLAIN QUERY PLAN` 必须命中索引、
  日汇总累加与覆盖式回填、回退路径、统计口径一致性、后台线程生命周期与异常隔离、
  设备删除级联、`--vacuum-now` 独占检测与压缩、性能冒烟
- 全量测试 197 passed（原 148 + 新增 49），Windows 10 + Python 3.12 实测通过

## v1.0.10 (2026-08-12)

### 修复：托盘菜单缺少「开机自启」

- 托盘右键菜单新增可勾选的「开机自启」项（原手册已写但代码未实现）：
  - 勾选 → 在 Windows 启动文件夹创建 `DEVICE LINK.lnk` 快捷方式
  - 取消 → 移除快捷方式
  - 启动时自动检测当前是否已启用并反映到勾选状态
- watchdog_manager 新增 `is_startup_shortcut_enabled()`，并统一快捷方式路径
  计算（winshell 与 win32com 回退共用同一路径），创建/移除/检测三方一致
- 新增回归测试 1 个（自启状态检测）

## v1.0.9 (2026-08-12)

### 新功能：每日 08:00 离线设备清单

- 告警与通知配置 → 告警合并策略新增「每日 08:00 发送离线设备清单」开关
- 每天 08:00 定时发送一封「当前离线设备清单」通知（邮件为主，启用的
  飞书/企微通道同步收到）
- 统计口径：只统计 `status='offline'` 的设备；待定（pending_failure）、
  维护中、未启用设备均不计入
- 当天离线设备为 0 台时不发送邮件，仅记录日志
- 每天只发送一次；程序当天重启不重复发送
  （状态记录于 `logs/daily_report_state.json`）
- 发送时间可通过 `config.yaml` 的 `notify.daily_report.send_time` 调整
- 清单内容：设备名、IP、子系统、最近探测时间、离线时长
- 三通道均支持 daily_report 事件渲染（邮件表格 / 飞书卡片 / 企微 markdown）

### 其他

- 用户手册全面重写：告警配置每个字段、完整告警流程、配置项速查、FAQ
  （docs/user_manual.md，详细度大幅提升）
- 新增回归测试 5 个（统计口径、发送/0 台跳过/开关/发送时间/防重复）

## v1.0.8 (2026-08-12)

### 修复：探测流程大量 "database is locked"（设备本身正常）

- 现象：1143 台设备下，日志持续出现"设备探测异常 ... database is locked"
  （约每分钟 10 次），但设备实际都在线。
- 根因：`record_check` 等写方法在语句失败（如 FOREIGN KEY——设备已被删除）
  后未回滚事务；sqlite3 遗留模式会让该连接保持事务打开、一直持有 WAL 写锁，
  其他线程写 5 秒（busy_timeout）后报 "database is locked"。
  "设备探测异常"是误导性文案——探测本身成功，失败的是状态落库。
- 修复：
  - 所有仓储写方法异常时自动回滚（`_db_sync` 统一处理），不再遗留持锁事务
  - 落库失败与探测失败区分日志："设备状态落库失败" vs "设备探测异常"
  - 探测结果先更新内存快照再落库（缓存先行）：DB 写失败不影响仪表盘/告警，
    两个统计不再因快照过期而不一致
  - FOREIGN KEY 失败（设备已删除）自动把设备移出调度器，避免残留

### 修复：告警配置保存卡死 5 分钟

- 根因：保存配置在 GUI 线程对每台设备逐条 `update_device()`（每次独立 commit），
  1143 台设备 × 探测写并发 = 数分钟，界面未响应。
- 修复：新增 `apply_global_thresholds_to_db()` 单事务批量 UPDATE（1 次 commit）；
  `apply_global_thresholds` 静默逐台更新（不再刷上千行日志）。

### 修复：仪表盘离线数与状态栏不一致

- 根因：已删除设备仍残留在调度器快照（dashboard 按 DB 设备列表渲染、
  状态栏按快照计数），以及落库失败时快照未更新。
- 修复：缓存先行 + FK 失败自动移出调度器，两个视图统一以调度器快照为准。

### 其他

- 新增回归测试 4 个（写失败回滚、批量阈值、缓存先行、FK 自动移除）
- 全量 142 passed

## v1.0.7 (2026-08-12)

### 修复：调度器卡死（无限杀/重启循环）

- 现象：设备数从 222 增至 1143 台（CSV 导入）后，调度器主循环心跳停更，
  看门狗 5 分钟内连续 7 次"调度器卡死"并自杀重启，重启后再次卡死。
- 根因：所有仓储读写共用一把全局数据库锁；调度主循环每轮对每台到期设备
  `get_device()` 都要排队抢写锁。上千台设备同时到期时锁队列拥塞，
  主循环被饿死 → 心跳停更 → 健康检查 `os._exit(1)` → 重启后同样洪峰重演。
- 修复：
  - 主循环与全局写锁解耦：只对"本轮提交的设备"做无锁读库刷新
    （WAL 并发读安全），心跳不再依赖锁获取
  - 单 tick 提交上限 `max_submit_per_tick`（默认 = max_workers），
    批量导入/启动后任务分批消化，不再瞬间涌入
  - 新增设备首轮探测在完整检查间隔内随机错峰（jitter_schedule）
  - `add_device` 幂等：重复同步只刷新缓存、不重置全部设备调度时间
    （原 GUI 每次增删改都会触发全体重排，是隐藏的惊群源）
  - 每次探测的状态写入与历史记录合并为单事务 `record_check`，
    写锁次数与 fsync 减半

### 看门狗重构（第 2 层独立进程）

- 第 2 层由"进程内守护线程"改为真正的独立子进程（`--watchdog` 模式）：
  主进程 `os._exit(1)` 后仍能拉起新实例（原实现线程随主进程一起死，
  重启路径实际从未生效，表现为无限重启）
- 重启计数/冷却/正常退出标记写入 `logs/watchdog_state.json`，跨重启生效；
  连续崩溃达 `max_restart_attempts` 次即停止重试，稳定运行超过
  `healthy_threshold_seconds`（默认 300s）自动复位计数
- 正常退出（关窗口/Ctrl+C）写 `clean_shutdown` 标记，看门狗不再误重启
- 冻结打包重启命令为 `[exe]`，开发模式为 `[python, src/main.py]`，透传原参数

### 其他

- 数据库锁粒度拆分：写方法保留全局锁，读方法不再加锁（WAL 并发读安全）
- 修复测试套件两个问题：健康检查测试线程残留导致全量 pytest 随机被杀；
  两个摘要测试的 sleep 时序边界（4→6、7→9）
- 新增回归测试 8 个（调度限流/缓存刷新/幂等/合并写、看门狗状态持久化/复位/退出标记）
- 全量 138 passed（连续两次完整通过）

## v1.0.6 (2026-08-09)

### 改进
- 告警引擎启动时若没有任何启用通知通道，日志由 INFO 升级为 WARNING，
  明确提示"真实告警不会发送"，避免用户误以为已配置完成
- 告警配置页"测试发送"成功后，若该通道未勾选"启用"，结果弹窗追加提醒：
  测试成功不代表真实告警会发送，需勾选启用并点击"保存配置"
  （防止再出现"日志显示发送成功但收不到告警"的误解）

## v1.0.5 (2026-08-09)

### 修复
- 飞书通知通道实际告警一直失败（错误码 11246 / ErrCode 11310 "unsupported type of block"）：
  - 根因：通道发送的是卡片 JSON 1.0 旧结构（顶层 `card.elements`、`is_short` div 块），
    而"测试发送"走的是纯文本消息，测试正常掩盖了卡片格式问题
  - 修复：`feishu_channel.py` 改为飞书现行卡片 JSON 2.0 标准结构
    （`schema: "2.0"` + `body.elements`，内容块为 `markdown`，分隔线为 `hr`），
    与自定义机器人官方文档示例一致
  - "测试发送"改为发送真实卡片（与告警同一条路径），不再用纯文本兜底，
    以后点测试即可真实校验卡片格式
  - 摘要清单（离线设备清单）以 markdown 内容块完整携带，多设备离线有明细
  - 单测新增飞书 2.0 结构断言与摘要卡片用例（131 passed）

## v1.0.4 (2026-08-09)

### 修复
- 告警升级邮件风暴（用户一夜收到 50 封）：
  - 根因：未确认的离线告警每 30 分钟无限重复发送升级邮件，且无关闭开关
  - 新增 `notify.escalation_enabled` 总开关（默认开），关闭后升级循环不再发送
  - 新增 `notify.escalation_max_count` 每事件升级次数上限（默认 3 次），
    达到上限后停止，防止无限轰炸
  - 告警配置页"升级时间"下拉新增"关闭"选项，保存即热生效（无需重启）
  - 升级循环线程改为随引擎停止（尊重 _running 标志）

## v1.0.3 (2026-08-08)

### 修复
- 告警邮件内容补全：单设备离线/恢复/升级通知现在包含设备名、IP 地址、系统名；
  合并摘要邮件/飞书/企微的离线清单补齐设备名、IP、系统名、时间，不再只有"几台设备离线"无明细
- 告警配置页"告警合并策略/全局告警规则"真正生效：
  - 合并窗口、单封摘要上限、紧急绕过、冷却窗口、升级时间保存后立即热更新（无需重启）
  - 全局失败阈值 N / 恢复阈值 M 保存后写入数据库并热应用到运行中的状态机
- 历史统计页"今天/7天/30天"按钮：修复白字白底（白框），并接通功能——
  时间范围选择会切换离线排行榜的统计周期（最近 1/7/30 天）
- 实时仪表盘/设备管理状态准确显示：
  - 维护模式设备显示"维护中"（不再显示"未知"）
  - 未启用设备显示"未启用"，状态灯按 维护中 > 未启用 > 探测状态 优先级解析

## v1.0.0 (2026-08-07)

### 新增
- ICMP Ping + TCP 端口双探测引擎
- 设备状态机（误报抑制 N 次判定 + 恢复阈值）
- 并发调度器（ThreadPoolExecutor，max_workers=50）
- 邮件/飞书/企业微信三通道通知
- 告警智能合并摘要（5 分钟窗口 + 紧急绕过）
- 升级机制（未确认告警自动升级）
- PySide6 仪表盘（按子系统分组设备卡片）
- 设备管理（CRUD + 批量操作 + CSV 导入导出）
- 历史统计（在线率 + 离线排行榜 + 告警日志）
- 看门狗守护（15 秒拉起）
- Nuitka 免安装打包
- 多尺寸 Logo/图标

### 修复
- add_device 默认值导致空字符串字段 → 使用合理 DEFAULTS
- SQLite 跨线程访问 → 连接池化（每线程独立 Connection）
- **BUG M（严重）**：看门狗 `os.kill(pid, 0)` 在 Windows 上实际杀死主进程
  - Unix 上信号 0 是空操作（仅检查进程存活）
  - Windows 上 `os.kill()` 调用 `TerminateProcess()` 杀死目标进程
  - 导致看门狗每 15 秒自杀一次
  - 修复：Windows 改用 `tasklist /FI "PID eq N"` 检查进程存活
- **sqlite3.dll ACCESS_VIOLATION (0xc0000005)**：跨线程共享同一 Connection 句柄
  - 修复：每线程通过 `threading.local()` 获取独立连接，去掉 `check_same_thread=False`
