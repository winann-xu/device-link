# Changelog

## v1.0.4 (2026-08-09)

### 修复
- 告警升级邮件风暴（用户一夜收到 50 封）：
  - 根因：未确认的离线告警每 30 分钟无限重复发送升级邮件，且无关闭开关
  - 新增 `notify.escalation_enabled` 总开关（默认开），关闭后升级循环不再发送
  - 新增 `notify.escalation_max_count` 每事件升级次数上限（默认 3 次），
    达到上限后停止，防止无限轰炸
  - 告警配置页"升级时间"下拉新增"关闭"选项，保存即热生效（无需重启）
  - 升级循环线程改为随引擎停止（尊重 _running 标志）

## v1.0.3 (2026-08-08)

### 修复
- 告警邮件内容补全：单设备离线/恢复/升级通知现在包含设备名、IP 地址、系统名；
  合并摘要邮件/飞书/企微的离线清单补齐设备名、IP、系统名、时间，不再只有"几台设备离线"无明细
- 告警配置页"告警合并策略/全局告警规则"真正生效：
  - 合并窗口、单封摘要上限、紧急绕过、冷却窗口、升级时间保存后立即热更新（无需重启）
  - 全局失败阈值 N / 恢复阈值 M 保存后写入数据库并热应用到运行中的状态机
- 历史统计页"今天/7天/30天"按钮：修复白字白底（白框），并接通功能——
  时间范围选择会切换离线排行榜的统计周期（最近 1/7/30 天）
- 实时仪表盘/设备管理状态准确显示：
  - 维护模式设备显示"维护中"（不再显示"未知"）
  - 未启用设备显示"未启用"，状态灯按 维护中 > 未启用 > 探测状态 优先级解析

## v1.0.0 (2026-08-07)

### 新增
- ICMP Ping + TCP 端口双探测引擎
- 设备状态机（误报抑制 N 次判定 + 恢复阈值）
- 并发调度器（ThreadPoolExecutor，max_workers=50）
- 邮件/飞书/企业微信三通道通知
- 告警智能合并摘要（5 分钟窗口 + 紧急绕过）
- 升级机制（未确认告警自动升级）
- PySide6 仪表盘（按子系统分组设备卡片）
- 设备管理（CRUD + 批量操作 + CSV 导入导出）
- 历史统计（在线率 + 离线排行榜 + 告警日志）
- 看门狗守护（15 秒拉起）
- Nuitka 免安装打包
- 多尺寸 Logo/图标

### 修复
- add_device 默认值导致空字符串字段 → 使用合理 DEFAULTS
- SQLite 跨线程访问 → 连接池化（每线程独立 Connection）
- **BUG M（严重）**：看门狗 `os.kill(pid, 0)` 在 Windows 上实际杀死主进程
  - Unix 上信号 0 是空操作（仅检查进程存活）
  - Windows 上 `os.kill()` 调用 `TerminateProcess()` 杀死目标进程
  - 导致看门狗每 15 秒自杀一次
  - 修复：Windows 改用 `tasklist /FI "PID eq N"` 检查进程存活
- **sqlite3.dll ACCESS_VIOLATION (0xc0000005)**：跨线程共享同一 Connection 句柄
  - 修复：每线程通过 `threading.local()` 获取独立连接，去掉 `check_same_thread=False`
